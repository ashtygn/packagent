"""LLM layer: provider abstraction, cassette replay, and the paranoia battery.

The LLM proposes; the deterministic pipeline (this package's battery) gates; the human
confirms. All five battery checks are deterministic code, not prompt instructions.
"""
